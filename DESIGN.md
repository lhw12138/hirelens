# HireLens Design System · 任务优先

保留蓝白的工作界面，精简旧版证据矩阵的默认高密度。HR在办公桌前长时间阅读简历，浅色背景与清晰正文优先。

## 用户路径

招聘任务 → 确认岗位要求 → 筛选简历 → 整理面试记录 → 综合评估与审核。
导航仅招聘任务、操作记录、使用帮助。技术信息在详情中。首页提供合成体验，已有任务标明下一步。

## 视觉规则

- 白色工作面、浅灰蓝背景；蓝 #2457d6 用于主要动作、当前步骤。
- 正文 #172b45，辅助文字 #56677d，边线 #dce3ec。错误使用浅暖背景和深红文字。
- 系统中文字体：正文15px，说明13–14px，标题22/30px。段落有足够行距。
- 顶部横向导航；主体最大1160px。主操作成组置于当前步骤末尾。
- 排名使用可展开的行；审核使用纵向维度和原文展开，避免三栏挤压。
- 使用8px控件圆角、14px主要工作面圆角、单层边框；不以卡片层层嵌套。
- 重点留白在任务阶段之间，相关标签与输入就近排列。

## 状态与响应式

保存中显示实际动作、禁用重复提交；成功明确“已保存”；失败保留输入并提供重试/重新读取。
步骤标签在手机上保留文字，两层顶部导航，不隐藏重要入口。表格改成可换行列表，正文不横向滚动。
键盘焦点、跳到主内容、输入标签、错误提示与读屏状态齐全。仅按钮颜色和步骤边线过渡，尊重减少动画设置。
